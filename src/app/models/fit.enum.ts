//src/app/models/fit.enum.ts
export enum FitPreference {
  OVERSIZED = 'Oversized & Relaxed',
  FITTED = 'Fitted & Sharp',
  MIX_MATCH = 'Mix & Match'
}
export const FIT_LISTA = Object.values(FitPreference);
